{
  "name": "bracelet-custom-configurator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Custom bracelet builder — embeddable React/Vite widget with realistic bead physics, designed for Shopify integration.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json && vite build",
    "build:widget": "tsc --noEmit -p tsconfig.json && vite build --mode widget",
    "generate:catalogue": "tsx scripts/generate-catalogue-assets.ts",
    "admin": "node --env-file=.env admin/server.mjs",
    "deploy:shopify": "npm run build:widget && node --env-file=.env scripts/deploy-shopify.mjs",
    "deploy:upload": "node --env-file=.env scripts/deploy-shopify.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "dependencies": {
    "@radix-ui/react-tabs": "^1.1.1",
    "lucide-react": "^0.456.0",
    "matter-js": "^0.20.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^4.5.5"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^0.1.65",
    "@types/matter-js": "^0.19.7",
    "@types/node": "^22.20.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^6.0.3",
    "autoprefixer": "^10.4.20",
    "clsx": "^2.1.1",
    "postcss": "^8.4.49",
    "tailwind-merge": "^2.5.4",
    "tailwindcss": "^3.4.15",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^8.1.0"
  }
}
